# Python
__pycache__/
*.py[cod]

# Training logs (noisy, regenerable)
checkpoints/**/*.log

# NOTE: model weights (*.pt) are ignored repo-wide by the parent DevoGraph
# .gitignore, so checkpoints are not versioned. Retrain with scripts/train_fate.py
# (config.json + history.json are committed so the run is reproducible).

# Large regenerable explainability dump (rebuild with scripts/extract_fate_explainability.py)
checkpoints/**/fate_explainability.json

# Duplicate PDFs at repo root — canonical copies live in References/
/2402.10793v3.pdf
/GSOC_Proposal.pdf

# Deployment bundles are assembled by scripts/build_api_bundle.py from
# src/, checkpoints/ and Dataset/, so the generated copies are not versioned.
api/src/
api/model/
space/src/
space/model/
space/service.py
space/examples/
